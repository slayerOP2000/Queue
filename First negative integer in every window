Approach
Simple approach hai hum logg phele deque lerhee hain we used 
deque overhere because ek baar sliding window mei answer add horha
ek baar remove fir hum logg check kr rhe ki meri value i se km horhii thii 
toh mai usko daaldunga  fir hum logg bchi hui window ko procee krenge
hum logg aage wali window ko remove kkrdenge and then we will be pushing cuurent element
 




deque<long long int> dq;
         vector<long long> ans;
         int negative = -1;
         
         //process first window
         for(int i=0; i<K; i++) {
             if(A[i] < 0) {
                 dq.push_back(i);
             }
         }
         
         //push ans for FIRST window
         if(dq.size() > 0) {
             ans.push_back(A[dq.front()]);
         }
         else
         {
             ans.push_back(0);
         }
         
         //now process for remaining windows
         for(int i = K; i<N; i++) {
             //first pop out of window element
             
             
             if(!dq.empty() && (i - dq.front())>=K ) {
                 dq.pop_front();
             }
             
             //then push current element
             if(A[i] < 0)
                dq.push_back(i);
             
            // put in ans
            if(dq.size() > 0) {
                 ans.push_back(A[dq.front()]);
            }
            else
            {
                ans.push_back(0);
            }
         }
         return ans;
